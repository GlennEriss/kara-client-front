@startuml
title Activité – Gérer onglets + stats demandes (Admin) – Crédit spéciale/fixe/aide
start
:Admin ouvre l'écran demandes;
:UI initialise onglets (pending, approved, rejected);
:Hooks charge stats par onglet (counts);
:Service calcule agrégations (pending, approved, rejected);
:Repo exécute requêtes groupées (where status);
:UI affiche onglets avec badges (counts);
if (Admin change d'onglet ?) then (oui)
  :Charger liste demandes de l'onglet;
  :Afficher filtres spécifiques à l'onglet;
  :Rafraîchir stats de l'onglet;
else (non)
endif
stop
@enduml

